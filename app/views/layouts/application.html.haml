!!!
%html
  %head
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
    %title NYC Dems
    /%meta{:content => content_for?(:description) ? yield(:description) : "Everplans", :name => "description"}
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    = yield(:head)
  %body
    #main{:role => "main"}
      .container
        = yield
      / ! end of .container
    / ! end of #main
